---
description: <one-line description of the constraint or reminder>
---
<!-- Add `paths:` only when this rule should be scoped to a folder — omit it
     for project-wide rules. `paths` is the ONLY other supported frontmatter
     key; `alwaysApply` and `globs` are not supported by this harness.
paths:
  - "agents/**"
-->

# <Topic>

<2-3 lines max — this file is ALWAYS loaded into every session, so state only
the constraint itself, not the reasoning or the procedure (that's the skill's job).>

Load the `<skill-name>` skill when:
- <concrete situation 1 — mirror the skill's own "Use when" so the two never drift>
- <concrete situation 2>

Skip the `<skill-name>` skill when:
- <read-only / unrelated task>
- <a case explicitly handled by a different, more specific skill instead>
