# SBSI Trading Web/App

> One of the product team's tracked projects — see the root `../../CLAUDE.md`
> for the parent brokerage context and repo-wide rules. This file only
> documents what is specific to the trading web/app product. **This folder
> holds knowledge, not code** — the actual codebase lives in its own repo;
> see `../README.md` for the convention.

## Project Context

<Fill in as you learn it. What is this product (web app, mobile app, or
both)? Which parts of SBSI's trading platform does it expose to end
customers — order entry, portfolio view, market data, account management?
Don't fabricate scope — leave this as a placeholder until confirmed.>

## Tech Stack

<Not decided yet from this repo's perspective — fill in once known.>

- **Frontend**: <...>
- **Mobile**: <...>
- **Backend/API it talks to**: <...>

## Where the code lives

<Link to the actual repo once it exists.>

## Domain Model

<Fill in once known — likely overlaps with the root `CLAUDE.md` domain
model placeholder (Customer -> Trading Account -> Order -> Execution ->
Settlement), but confirm against this product's actual API/schema rather
than assuming.>

## Issue Tracking

<Fill in the Linear team/project once confirmed — see root
`.claude/rules/linear.md` for the MCP-based workflow.>
